# Dependências de runtime (API e pipeline de dados)
fastapi>=0.140.13
uvicorn[standard]>=0.52.0
# Teto de major em pandas/xlrd: são eles que definem os bytes dos CSVs, e o
# teste de reprodutibilidade compara byte a byte. O teto não substitui o teste,
# só transforma "o CI quebrou sozinho" em "o Dependabot propôs a subida".
pandas>=2.3.3,<4
xlrd>=2.0.2,<3  # leitura dos arquivos .xls originais (pipeline)
